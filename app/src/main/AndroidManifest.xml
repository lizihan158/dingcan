<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication200">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication200">

        <!-- 登录页面，变为启动页 -->
        <activity
            android:name=".ui.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.MainActivity" android:exported="false"/>
        <activity android:name=".ui.activity.ShopDetailActivity" android:exported="false"/>
        <activity android:name=".ui.activity.OrderActivity" android:exported="false"/>
        <activity android:name=".ui.activity.PayActivity" android:exported="false"/>
        <activity android:name=".ui.activity.BigImageActivity" android:exported="false"/>
        <activity android:name=".ui.activity.QrCodeActivity" android:exported="false"/>

    </application>
</manifest>


